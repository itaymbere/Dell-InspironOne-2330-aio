conky.config = {
--=============================================================================
--                        Dell-InspironOne-2330_aio
-- Date    : 22/05/2026
-- Author  : Itaymbere
-- Version : v0.1
-- License : Distributed under the terms of GNU GPL v3 or later
-- 
-- This version is a modification of conkyrc_Gotam
-- 
-- Github:    https://github.com/itaymbere/.conky.git
-- 
--=============================================================================

    background = false,
    font = 'FuraCode Nerd Font:pixelsize=8',
    use_xft = true,
    xftalpha = 1.0,
    update_interval = 4,
    total_run_times = 0,
    own_window = true,
    own_window_transparent = true,
    own_window_type = 'dock',
    own_window_class = 'Conky',
    own_window_argb_visual = true,
    own_window_argb_value = 255,
    own_window_colour = '000000',
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
    double_buffer = true,
    draw_shades = false,
    draw_outline = false,
    draw_borders = false,
    draw_graph_borders = true,
    stippled_borders = 0,
    border_width = 0,
    default_color = '#FFFFFF',
    default_shade_color = '#FFFFFF',
    default_outline_color = '#FFFFFF',
    alignment = 'middle_right',
    gap_x = 0,
    gap_y = -75,
    no_buffers = true,
    cpu_avg_samples = 2,
    net_avg_samples = 2,
    use_spacer = 'none',
    maximum_width = 1024,
    minimum_height = 640,
    uppercase = true,
};

conky.text = [[
${alignr}Processes           CPU
${alignr}${top name 1}   ${top cpu 1}
${alignr}${top name 2}   ${top cpu 2}
${alignr}${top name 3}   ${top cpu 2}
${alignr}${top name 4}   ${top cpu 2}
]];
